package ImgBell.Auth.JWT;


import lombok.RequiredArgsConstructor;
import org.springframework.web.bind.annotation.*;

// GEMINI: ImgBell은 리소스 서버이므로 인증(로그인, 토큰 발급) 관련 엔드포인트를 모두 제거합니다.
// GEMINI: 이 역할은 인증 서버인 AuthBell이 담당합니다.
@RestController
@RequiredArgsConstructor // Lombok을 사용하여 생성자 주입
public class JWTController {

    // GEMINI: 모든 내용이 제거되었으므로 이 컨트롤러는 현재 비어있습니다.
    // GEMINI: 추후 다른 용도로 사용하거나, 필요 없다면 파일을 삭제해도 무방합니다.

}
